"use client";
import Link from "next/link";
import { Heart, Menu, Search, ShoppingCart, UserRound } from "lucide-react";
import { useMarketplace } from "./marketplace-provider";
import { Brand } from "./ui";
const navigation = ["All Products", "Pharmaceuticals", "Cosmetics & Beauty", "Food & Beverages", "Electronics", "Fashion", "Home & Living", "Sports"];
export function Header() { const { cartCount } = useMarketplace(); return <header className="bg-neutral-900 text-white"><div className="mx-auto flex max-w-[1600px] items-center gap-5 px-5 py-3"><Link href="/"><Brand dark /></Link><div className="hidden flex-1 items-center rounded-full border border-neutral-500 bg-neutral-800/60 px-5 py-3 md:flex"><Search className="mr-2 size-5" /><input className="w-full bg-transparent text-sm outline-none placeholder:text-neutral-300" placeholder="Search products..." /></div><div className="ml-auto flex items-center gap-3"><Heart className="size-4 transition hover:scale-110 hover:text-red-400" /><span className="relative"><ShoppingCart className="size-4" /><i className="absolute -right-2 -top-2 grid size-4 place-items-center rounded-full bg-sky-200 text-[9px] not-italic text-neutral-950">{cartCount}</i></span><UserRound className="size-4" /><Link href="/seller/listings/new" className="hidden rounded-full bg-sky-200 px-4 py-2 text-[10px] font-black text-neutral-950 transition hover:bg-sky-100 active:scale-95 sm:block">SELL ON MARQETPLACE</Link><Menu className="size-5 md:hidden" /></div></div><nav className="hidden border-t border-neutral-700 px-6 md:block"><div className="mx-auto flex max-w-[1600px] items-center justify-between overflow-x-auto">{navigation.map((item, index) => <Link key={item} href={index === 0 ? "/" : `/?category=${encodeURIComponent(item)}`} className={`shrink-0 border-r border-neutral-700 px-6 py-5 text-[11px] font-black uppercase tracking-wider transition hover:bg-neutral-800 ${index === 0 ? "text-sky-200" : "text-neutral-300"}`}>{item}</Link>)}</div></nav></header>; }
